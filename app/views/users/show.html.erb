<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Screen name:</b>
  <%= @user.screen_name %>
</p>

<%#= Twitter.followers('@user.screen_name') %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<form action="/user/<%= @user.id%>/check_if_follows" method='get'>
	<label>user2</label>
	<input type="text" name='user2'/>
	<input type="submit" name="Find out" >
</form>

<%#= @following = @followers.where(:id => User.first) %>

<ul class='tweets'>
  <% @followers.all.each do |follower| %>
    <li><%= follower %></li>
  <% end %>
</ul>

